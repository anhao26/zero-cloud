// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/anhao26/zero-cloud/service/ticket/ticket-rpc/ent/attributeset"
	"github.com/anhao26/zero-cloud/service/ticket/ticket-rpc/ent/predicate"
)

// AttributeSetQuery is the builder for querying AttributeSet entities.
type AttributeSetQuery struct {
	config
	ctx        *QueryContext
	order      []attributeset.OrderOption
	inters     []Interceptor
	predicates []predicate.AttributeSet
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AttributeSetQuery builder.
func (asq *AttributeSetQuery) Where(ps ...predicate.AttributeSet) *AttributeSetQuery {
	asq.predicates = append(asq.predicates, ps...)
	return asq
}

// Limit the number of records to be returned by this query.
func (asq *AttributeSetQuery) Limit(limit int) *AttributeSetQuery {
	asq.ctx.Limit = &limit
	return asq
}

// Offset to start from.
func (asq *AttributeSetQuery) Offset(offset int) *AttributeSetQuery {
	asq.ctx.Offset = &offset
	return asq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (asq *AttributeSetQuery) Unique(unique bool) *AttributeSetQuery {
	asq.ctx.Unique = &unique
	return asq
}

// Order specifies how the records should be ordered.
func (asq *AttributeSetQuery) Order(o ...attributeset.OrderOption) *AttributeSetQuery {
	asq.order = append(asq.order, o...)
	return asq
}

// First returns the first AttributeSet entity from the query.
// Returns a *NotFoundError when no AttributeSet was found.
func (asq *AttributeSetQuery) First(ctx context.Context) (*AttributeSet, error) {
	nodes, err := asq.Limit(1).All(setContextOp(ctx, asq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{attributeset.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (asq *AttributeSetQuery) FirstX(ctx context.Context) *AttributeSet {
	node, err := asq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AttributeSet ID from the query.
// Returns a *NotFoundError when no AttributeSet ID was found.
func (asq *AttributeSetQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = asq.Limit(1).IDs(setContextOp(ctx, asq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{attributeset.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (asq *AttributeSetQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := asq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AttributeSet entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AttributeSet entity is found.
// Returns a *NotFoundError when no AttributeSet entities are found.
func (asq *AttributeSetQuery) Only(ctx context.Context) (*AttributeSet, error) {
	nodes, err := asq.Limit(2).All(setContextOp(ctx, asq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{attributeset.Label}
	default:
		return nil, &NotSingularError{attributeset.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (asq *AttributeSetQuery) OnlyX(ctx context.Context) *AttributeSet {
	node, err := asq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AttributeSet ID in the query.
// Returns a *NotSingularError when more than one AttributeSet ID is found.
// Returns a *NotFoundError when no entities are found.
func (asq *AttributeSetQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = asq.Limit(2).IDs(setContextOp(ctx, asq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{attributeset.Label}
	default:
		err = &NotSingularError{attributeset.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (asq *AttributeSetQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := asq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AttributeSets.
func (asq *AttributeSetQuery) All(ctx context.Context) ([]*AttributeSet, error) {
	ctx = setContextOp(ctx, asq.ctx, "All")
	if err := asq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AttributeSet, *AttributeSetQuery]()
	return withInterceptors[[]*AttributeSet](ctx, asq, qr, asq.inters)
}

// AllX is like All, but panics if an error occurs.
func (asq *AttributeSetQuery) AllX(ctx context.Context) []*AttributeSet {
	nodes, err := asq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AttributeSet IDs.
func (asq *AttributeSetQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if asq.ctx.Unique == nil && asq.path != nil {
		asq.Unique(true)
	}
	ctx = setContextOp(ctx, asq.ctx, "IDs")
	if err = asq.Select(attributeset.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (asq *AttributeSetQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := asq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (asq *AttributeSetQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, asq.ctx, "Count")
	if err := asq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, asq, querierCount[*AttributeSetQuery](), asq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (asq *AttributeSetQuery) CountX(ctx context.Context) int {
	count, err := asq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (asq *AttributeSetQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, asq.ctx, "Exist")
	switch _, err := asq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (asq *AttributeSetQuery) ExistX(ctx context.Context) bool {
	exist, err := asq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AttributeSetQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (asq *AttributeSetQuery) Clone() *AttributeSetQuery {
	if asq == nil {
		return nil
	}
	return &AttributeSetQuery{
		config:     asq.config,
		ctx:        asq.ctx.Clone(),
		order:      append([]attributeset.OrderOption{}, asq.order...),
		inters:     append([]Interceptor{}, asq.inters...),
		predicates: append([]predicate.AttributeSet{}, asq.predicates...),
		// clone intermediate query.
		sql:  asq.sql.Clone(),
		path: asq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AttributeSet.Query().
//		GroupBy(attributeset.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (asq *AttributeSetQuery) GroupBy(field string, fields ...string) *AttributeSetGroupBy {
	asq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AttributeSetGroupBy{build: asq}
	grbuild.flds = &asq.ctx.Fields
	grbuild.label = attributeset.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.AttributeSet.Query().
//		Select(attributeset.FieldCreatedAt).
//		Scan(ctx, &v)
func (asq *AttributeSetQuery) Select(fields ...string) *AttributeSetSelect {
	asq.ctx.Fields = append(asq.ctx.Fields, fields...)
	sbuild := &AttributeSetSelect{AttributeSetQuery: asq}
	sbuild.label = attributeset.Label
	sbuild.flds, sbuild.scan = &asq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AttributeSetSelect configured with the given aggregations.
func (asq *AttributeSetQuery) Aggregate(fns ...AggregateFunc) *AttributeSetSelect {
	return asq.Select().Aggregate(fns...)
}

func (asq *AttributeSetQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range asq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, asq); err != nil {
				return err
			}
		}
	}
	for _, f := range asq.ctx.Fields {
		if !attributeset.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if asq.path != nil {
		prev, err := asq.path(ctx)
		if err != nil {
			return err
		}
		asq.sql = prev
	}
	return nil
}

func (asq *AttributeSetQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AttributeSet, error) {
	var (
		nodes = []*AttributeSet{}
		_spec = asq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AttributeSet).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AttributeSet{config: asq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, asq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (asq *AttributeSetQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := asq.querySpec()
	_spec.Node.Columns = asq.ctx.Fields
	if len(asq.ctx.Fields) > 0 {
		_spec.Unique = asq.ctx.Unique != nil && *asq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, asq.driver, _spec)
}

func (asq *AttributeSetQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(attributeset.Table, attributeset.Columns, sqlgraph.NewFieldSpec(attributeset.FieldID, field.TypeUint64))
	_spec.From = asq.sql
	if unique := asq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if asq.path != nil {
		_spec.Unique = true
	}
	if fields := asq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, attributeset.FieldID)
		for i := range fields {
			if fields[i] != attributeset.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := asq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := asq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := asq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := asq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (asq *AttributeSetQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(asq.driver.Dialect())
	t1 := builder.Table(attributeset.Table)
	columns := asq.ctx.Fields
	if len(columns) == 0 {
		columns = attributeset.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if asq.sql != nil {
		selector = asq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if asq.ctx.Unique != nil && *asq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range asq.predicates {
		p(selector)
	}
	for _, p := range asq.order {
		p(selector)
	}
	if offset := asq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := asq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AttributeSetGroupBy is the group-by builder for AttributeSet entities.
type AttributeSetGroupBy struct {
	selector
	build *AttributeSetQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (asgb *AttributeSetGroupBy) Aggregate(fns ...AggregateFunc) *AttributeSetGroupBy {
	asgb.fns = append(asgb.fns, fns...)
	return asgb
}

// Scan applies the selector query and scans the result into the given value.
func (asgb *AttributeSetGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, asgb.build.ctx, "GroupBy")
	if err := asgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributeSetQuery, *AttributeSetGroupBy](ctx, asgb.build, asgb, asgb.build.inters, v)
}

func (asgb *AttributeSetGroupBy) sqlScan(ctx context.Context, root *AttributeSetQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(asgb.fns))
	for _, fn := range asgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*asgb.flds)+len(asgb.fns))
		for _, f := range *asgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*asgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := asgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AttributeSetSelect is the builder for selecting fields of AttributeSet entities.
type AttributeSetSelect struct {
	*AttributeSetQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ass *AttributeSetSelect) Aggregate(fns ...AggregateFunc) *AttributeSetSelect {
	ass.fns = append(ass.fns, fns...)
	return ass
}

// Scan applies the selector query and scans the result into the given value.
func (ass *AttributeSetSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ass.ctx, "Select")
	if err := ass.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AttributeSetQuery, *AttributeSetSelect](ctx, ass.AttributeSetQuery, ass, ass.inters, v)
}

func (ass *AttributeSetSelect) sqlScan(ctx context.Context, root *AttributeSetQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ass.fns))
	for _, fn := range ass.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ass.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ass.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
