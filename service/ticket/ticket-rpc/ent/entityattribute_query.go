// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/anhao26/zero-cloud/service/ticket/ticket-rpc/ent/entityattribute"
	"github.com/anhao26/zero-cloud/service/ticket/ticket-rpc/ent/predicate"
)

// EntityAttributeQuery is the builder for querying EntityAttribute entities.
type EntityAttributeQuery struct {
	config
	ctx        *QueryContext
	order      []entityattribute.OrderOption
	inters     []Interceptor
	predicates []predicate.EntityAttribute
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EntityAttributeQuery builder.
func (eaq *EntityAttributeQuery) Where(ps ...predicate.EntityAttribute) *EntityAttributeQuery {
	eaq.predicates = append(eaq.predicates, ps...)
	return eaq
}

// Limit the number of records to be returned by this query.
func (eaq *EntityAttributeQuery) Limit(limit int) *EntityAttributeQuery {
	eaq.ctx.Limit = &limit
	return eaq
}

// Offset to start from.
func (eaq *EntityAttributeQuery) Offset(offset int) *EntityAttributeQuery {
	eaq.ctx.Offset = &offset
	return eaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eaq *EntityAttributeQuery) Unique(unique bool) *EntityAttributeQuery {
	eaq.ctx.Unique = &unique
	return eaq
}

// Order specifies how the records should be ordered.
func (eaq *EntityAttributeQuery) Order(o ...entityattribute.OrderOption) *EntityAttributeQuery {
	eaq.order = append(eaq.order, o...)
	return eaq
}

// First returns the first EntityAttribute entity from the query.
// Returns a *NotFoundError when no EntityAttribute was found.
func (eaq *EntityAttributeQuery) First(ctx context.Context) (*EntityAttribute, error) {
	nodes, err := eaq.Limit(1).All(setContextOp(ctx, eaq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{entityattribute.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eaq *EntityAttributeQuery) FirstX(ctx context.Context) *EntityAttribute {
	node, err := eaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EntityAttribute ID from the query.
// Returns a *NotFoundError when no EntityAttribute ID was found.
func (eaq *EntityAttributeQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = eaq.Limit(1).IDs(setContextOp(ctx, eaq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{entityattribute.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eaq *EntityAttributeQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := eaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EntityAttribute entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EntityAttribute entity is found.
// Returns a *NotFoundError when no EntityAttribute entities are found.
func (eaq *EntityAttributeQuery) Only(ctx context.Context) (*EntityAttribute, error) {
	nodes, err := eaq.Limit(2).All(setContextOp(ctx, eaq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{entityattribute.Label}
	default:
		return nil, &NotSingularError{entityattribute.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eaq *EntityAttributeQuery) OnlyX(ctx context.Context) *EntityAttribute {
	node, err := eaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EntityAttribute ID in the query.
// Returns a *NotSingularError when more than one EntityAttribute ID is found.
// Returns a *NotFoundError when no entities are found.
func (eaq *EntityAttributeQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = eaq.Limit(2).IDs(setContextOp(ctx, eaq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{entityattribute.Label}
	default:
		err = &NotSingularError{entityattribute.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eaq *EntityAttributeQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := eaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EntityAttributes.
func (eaq *EntityAttributeQuery) All(ctx context.Context) ([]*EntityAttribute, error) {
	ctx = setContextOp(ctx, eaq.ctx, "All")
	if err := eaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EntityAttribute, *EntityAttributeQuery]()
	return withInterceptors[[]*EntityAttribute](ctx, eaq, qr, eaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eaq *EntityAttributeQuery) AllX(ctx context.Context) []*EntityAttribute {
	nodes, err := eaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EntityAttribute IDs.
func (eaq *EntityAttributeQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if eaq.ctx.Unique == nil && eaq.path != nil {
		eaq.Unique(true)
	}
	ctx = setContextOp(ctx, eaq.ctx, "IDs")
	if err = eaq.Select(entityattribute.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eaq *EntityAttributeQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := eaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eaq *EntityAttributeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eaq.ctx, "Count")
	if err := eaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eaq, querierCount[*EntityAttributeQuery](), eaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eaq *EntityAttributeQuery) CountX(ctx context.Context) int {
	count, err := eaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eaq *EntityAttributeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eaq.ctx, "Exist")
	switch _, err := eaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eaq *EntityAttributeQuery) ExistX(ctx context.Context) bool {
	exist, err := eaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EntityAttributeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eaq *EntityAttributeQuery) Clone() *EntityAttributeQuery {
	if eaq == nil {
		return nil
	}
	return &EntityAttributeQuery{
		config:     eaq.config,
		ctx:        eaq.ctx.Clone(),
		order:      append([]entityattribute.OrderOption{}, eaq.order...),
		inters:     append([]Interceptor{}, eaq.inters...),
		predicates: append([]predicate.EntityAttribute{}, eaq.predicates...),
		// clone intermediate query.
		sql:  eaq.sql.Clone(),
		path: eaq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EntityAttribute.Query().
//		GroupBy(entityattribute.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eaq *EntityAttributeQuery) GroupBy(field string, fields ...string) *EntityAttributeGroupBy {
	eaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EntityAttributeGroupBy{build: eaq}
	grbuild.flds = &eaq.ctx.Fields
	grbuild.label = entityattribute.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.EntityAttribute.Query().
//		Select(entityattribute.FieldCreatedAt).
//		Scan(ctx, &v)
func (eaq *EntityAttributeQuery) Select(fields ...string) *EntityAttributeSelect {
	eaq.ctx.Fields = append(eaq.ctx.Fields, fields...)
	sbuild := &EntityAttributeSelect{EntityAttributeQuery: eaq}
	sbuild.label = entityattribute.Label
	sbuild.flds, sbuild.scan = &eaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EntityAttributeSelect configured with the given aggregations.
func (eaq *EntityAttributeQuery) Aggregate(fns ...AggregateFunc) *EntityAttributeSelect {
	return eaq.Select().Aggregate(fns...)
}

func (eaq *EntityAttributeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eaq); err != nil {
				return err
			}
		}
	}
	for _, f := range eaq.ctx.Fields {
		if !entityattribute.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eaq.path != nil {
		prev, err := eaq.path(ctx)
		if err != nil {
			return err
		}
		eaq.sql = prev
	}
	return nil
}

func (eaq *EntityAttributeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EntityAttribute, error) {
	var (
		nodes = []*EntityAttribute{}
		_spec = eaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EntityAttribute).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EntityAttribute{config: eaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (eaq *EntityAttributeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eaq.querySpec()
	_spec.Node.Columns = eaq.ctx.Fields
	if len(eaq.ctx.Fields) > 0 {
		_spec.Unique = eaq.ctx.Unique != nil && *eaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eaq.driver, _spec)
}

func (eaq *EntityAttributeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(entityattribute.Table, entityattribute.Columns, sqlgraph.NewFieldSpec(entityattribute.FieldID, field.TypeUint64))
	_spec.From = eaq.sql
	if unique := eaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eaq.path != nil {
		_spec.Unique = true
	}
	if fields := eaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, entityattribute.FieldID)
		for i := range fields {
			if fields[i] != entityattribute.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eaq *EntityAttributeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eaq.driver.Dialect())
	t1 := builder.Table(entityattribute.Table)
	columns := eaq.ctx.Fields
	if len(columns) == 0 {
		columns = entityattribute.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eaq.sql != nil {
		selector = eaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eaq.ctx.Unique != nil && *eaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eaq.predicates {
		p(selector)
	}
	for _, p := range eaq.order {
		p(selector)
	}
	if offset := eaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EntityAttributeGroupBy is the group-by builder for EntityAttribute entities.
type EntityAttributeGroupBy struct {
	selector
	build *EntityAttributeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eagb *EntityAttributeGroupBy) Aggregate(fns ...AggregateFunc) *EntityAttributeGroupBy {
	eagb.fns = append(eagb.fns, fns...)
	return eagb
}

// Scan applies the selector query and scans the result into the given value.
func (eagb *EntityAttributeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eagb.build.ctx, "GroupBy")
	if err := eagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EntityAttributeQuery, *EntityAttributeGroupBy](ctx, eagb.build, eagb, eagb.build.inters, v)
}

func (eagb *EntityAttributeGroupBy) sqlScan(ctx context.Context, root *EntityAttributeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(eagb.fns))
	for _, fn := range eagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*eagb.flds)+len(eagb.fns))
		for _, f := range *eagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*eagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EntityAttributeSelect is the builder for selecting fields of EntityAttribute entities.
type EntityAttributeSelect struct {
	*EntityAttributeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (eas *EntityAttributeSelect) Aggregate(fns ...AggregateFunc) *EntityAttributeSelect {
	eas.fns = append(eas.fns, fns...)
	return eas
}

// Scan applies the selector query and scans the result into the given value.
func (eas *EntityAttributeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eas.ctx, "Select")
	if err := eas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EntityAttributeQuery, *EntityAttributeSelect](ctx, eas.EntityAttributeQuery, eas, eas.inters, v)
}

func (eas *EntityAttributeSelect) sqlScan(ctx context.Context, root *EntityAttributeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(eas.fns))
	for _, fn := range eas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*eas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
